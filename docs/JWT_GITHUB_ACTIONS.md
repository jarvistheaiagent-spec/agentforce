# JWT Auth for GitHub Actions (Salesforce)

We attempted JWT auth, but this org doesn't allow creating a classic Connected App.

Use the SFDX Auth URL flow instead:
- See `docs/AUTH_GITHUB_ACTIONS_SFDX_URL.md`
